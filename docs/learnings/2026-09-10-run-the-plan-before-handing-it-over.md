# Reading an implementation plan does not find the defects that running it finds

ts: 2026-09-10T04:43:40Z
commit: c6bafff0487349dc065b85ab7f1e255e4c3384b8
session: https://claude.ai/code/session_01LvzR5v7WVozJWMPtMG7M1F
status: verified
fact: A plan whose steps promise "run this, expect N passed" is making a claim, and the only way to check it is to extract every labelled code block into a scratch tree, apply each prose Modify instruction literally, and run the suite. Doing that to this repository's build plan surfaced eleven defects that two prose reviews had not: a twin filename collision that silently dropped a row, a floor introduced in one part that broke a test in an earlier part, three tests using a runner name a later rule rejects, a stage test measuring a laptop file copy against a rented-instance floor, a template assertion naming a token the template did not use, thirty-two invalid Terraform blocks, a script that could not import its own repository root when run as a script, a test helper keyword collision, a four-hex hash checked against a sixty-four-hex pattern, and a temp directory never created before `git init`. Each would have cost a debugging cycle during execution; several would have surfaced only with a GPU billing.
basis: extraction and run of the plan at md5 `3d5097511bcdfc8a709a9742ac202f83` reported `100 passed in 16.99s`; the first such run, before the fixes, reported `9 failed, 52 passed` on Part 1 alone and `8 failed, 92 passed` after Parts 2 and 3 were added. Prose-only corrections made later the same day changed the plan's md5 to `a3ebfe549a7306f317b0001a87b543e9`, and the suite re-run against that text again reported `100 passed`. The extractor and the instruction-applier lived in the session scratchpad and do not survive it; the durable evidence is the defect list above, each item traceable to a specific passage of the plan.
re-verify: sed -n '/^## Dry run of this document/,/^Not exercised/p' ~/dev/traverse/docs/plans/2026-09-09-full-build.md
